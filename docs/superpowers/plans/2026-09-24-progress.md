# AVICOLA PRO - progreso verificado (2026-09-24)

## Hecho en este corte

- Producción diaria de huevos por lote/galpón, conteos enteros e idempotencia; clasificación explícita contra categorías configuradas y recepción mediante el ledger existente.
- Configuración de categorías vendibles/no vendibles, conversiones versionadas sin tamaño de bandeja predeterminado, saldos por almacén y bloqueo de venta cuando la categoría no sea vendible.
- Canales mayorista/minorista en pedidos y facturas nuevas; notas de crédito heredan el canal original. Los registros anteriores quedan sin clasificar, sin backfill supuesto.
- Reporte de ventas por canal con facturas y notas de crédito neteadas. Los costos y márgenes no aprobados ya no se presentan como cero/ganancia ficticia.
- Exportación XLSX acotada, protegida por permiso y auditada; cadenas de datos se escriben como texto, no como fórmulas.
- Smoke de Compose D11/D12 ahora comprueba el head de migración `0013_sales_channel` en vez de `0010_costing`.

## Verificaciones locales ejecutadas

- Backend: `245 passed, 49 deselected` para la suite que excluye integración; Ruff, formato y Mypy pasan.
- Alembic: generación offline desde base hasta `0013_sales_channel` pasa.
- Frontend: 54 pruebas; 87.76% statements, 80.05% branches, 92.4% functions y 90.28% lines; lint, typecheck y build optimizado pasan.
- La suite PostgreSQL real no se puede ejecutar en esta máquina porque no hay servidor PostgreSQL/Docker disponible. Se requiere el job `postgresql-integration` de GitHub Actions.

## Pendiente para declarar completo

- Ejecutar y revisar CI completo sobre el SHA final; debe producir/restaurar el backup sintético, pasar escaneos y generar/verificar bundle atestado. No está certificado hasta que todos los jobs estén verdes.
- Ejecutar contra PostgreSQL las migraciones `0011`-`0013`, restricciones, reversión y transacciones.
- Completar el formulario de creación de venta en interfaz: el panel existente era solo lectura; ahora filtra y muestra canales, mientras la API sí los exige.
- Aprobar con el negocio las definiciones y ejemplos de porcentaje de postura, consumo/ave, conversión, costos por huevo, margen, cobertura, cliente nuevo y ticket promedio. Se mantienen fuera de los cálculos oficiales.
- Capturar una imagen genuina del sistema ejecutándose con datos sintéticos y generar el ZIP excluyendo secretos/entornos; enviar el correo solo al cerrar todas las verificaciones.
- No desplegar a producción.

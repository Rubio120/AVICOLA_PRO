# AVICOLA PRO - progreso verificado (2026-09-24)

## Hecho en este corte

- Producci�n diaria de huevos por lote/galp�n, conteos enteros e idempotencia; clasificaci�n expl�cita contra categor�as configuradas y recepci�n mediante el ledger existente.
- Configuraci�n de categor�as vendibles/no vendibles, conversiones versionadas sin tama�o de bandeja predeterminado, saldos por almac�n y bloqueo de venta cuando la categor�a no sea vendible.
- Canales mayorista/minorista en pedidos y facturas nuevas; notas de cr�dito heredan el canal original. Los registros anteriores quedan sin clasificar, sin backfill supuesto.
- Reporte de ventas por canal con facturas y notas de cr�dito neteadas. Los costos y m�rgenes no aprobados ya no se presentan como cero/ganancia ficticia.
- Exportaci�n XLSX acotada, protegida por permiso y auditada; cadenas de datos se escriben como texto, no como f�rmulas.
- Smoke de Compose D11/D12 ahora comprueba el head de migraci�n `0013_sales_channel` en vez de `0010_costing`.

## Verificaciones locales ejecutadas

- Backend: `245 passed, 49 deselected` para la suite que excluye integraci�n; Ruff, formato y Mypy pasan.
- Alembic: generaci�n offline desde base hasta `0013_sales_channel` pasa.
- Frontend: 54 pruebas; 87.76% statements, 80.05% branches, 92.4% functions y 90.28% lines; lint, typecheck y build optimizado pasan.
- La suite PostgreSQL real no se puede ejecutar en esta m�quina porque no hay servidor PostgreSQL/Docker disponible. Se requiere el job `postgresql-integration` de GitHub Actions.

## Estado de verificacion al 2026-09-25

- Backend completo contra PostgreSQL sintetico: `345 passed`, cobertura `80.75%`. Pytest uso un basetemp aislado.
- Integraciones focalizadas recientes de produccion/inventario/reporting: `12 passed`; unit tests focalizados: `29 passed`.
- Frontend completo: `60 passed`; cobertura `88.08%` statements y `80.93%` branches. ESLint focalizado y `next build` pasan.
- Se aclaro la metrica del lote: dias desde el ingreso, no edad biologica de las aves (no se captura edad inicial).
- CI remoto conocido del SHA anterior: PostgreSQL, Windows, fuente, dependencias y Compose pasan; `deployment-images` falla por HIGH/CRITICAL en paquetes Debian. El gate fail-closed omite bundle y verificacion. No se rebajo el umbral; elegir otra base o esperar parches requiere decision.
- Pendiente: CI verde del SHA final y bundle/attestation verificados; captura visual sintetica; definicion de costos indirectos/devoluciones para costo total y margen; ZIP limpio y envio por correo cuando todo lo anterior pase.
- No desplegar a produccion.

## Pendiente para declarar completo (corte anterior; ver estado de verificacion arriba)

- Ejecutar y revisar CI completo sobre el SHA final; debe producir/restaurar el backup sint�tico, pasar escaneos y generar/verificar bundle atestado. No est� certificado hasta que todos los jobs est�n verdes.
- Ejecutar contra PostgreSQL las migraciones `0011`-`0013`, restricciones, reversi�n y transacciones.
- Completar el formulario de creaci�n de venta en interfaz: el panel existente era solo lectura; ahora filtra y muestra canales, mientras la API s� los exige.
- Aprobar con el negocio las definiciones y ejemplos de porcentaje de postura, consumo/ave, conversi�n, costos por huevo, margen, cobertura, cliente nuevo y ticket promedio. Se mantienen fuera de los c�lculos oficiales.
- Capturar una imagen genuina del sistema ejecut�ndose con datos sint�ticos y generar el ZIP excluyendo secretos/entornos; enviar el correo solo al cerrar todas las verificaciones.
- No desplegar a producci�n.

